apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: legion-ingress
spec:
  rules:
  - host: legion.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: legion-service
            port:
              number: 3000
